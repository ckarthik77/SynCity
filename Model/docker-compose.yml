services:
  syncity-dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./models:/app/models
      - ./realtime_predictions.csv:/app/realtime_predictions.csv
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
    restart: unless-stopped